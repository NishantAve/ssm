{
    "name": "aws-ssm-send-command",
    "version": "1.0.0",
    "description": "GitHub Actions for executing AWS SSM Send Command",
    "main": "dist/index.js",
    "type": "module",
    "scripts": {
        "build": "tsc",
        "prepare": "npm run build",
        "clean": "rm -rf dist/",
        "test": "echo \"Error: no test specified\" && exit 1"
    },
    "keywords": [
        "github-actions",
        "aws-ssm",
        "remote-command-execution"
    ],
    "dependencies": {
        "@actions/core": "^1.10.1",
        "@actions/github": "^6.0.0",
        "aws-sdk": "^2.1589.0",
        "node-fetch": "^3.3.2"
    },
    "devDependencies": {
        "typescript": "^5.4.3"
    }
}
